import {makeStyles} from "@mui/styles";

export default makeStyles(()=>({
    footer : {
        
        display:"flex",
        flexDirection:"column",
        alignItems:"center",
        justifyContent:"center",
        color:"black",
    }
}));